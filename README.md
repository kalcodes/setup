## Setup
A repository containing custom scripts, configurations, and guides. 

**Installation:**
```bash
cd ~
git clone https://github.com/kalcodes/setup.git
cd setup

chmod +x install
./install 
```
